import { PluginBase } from '@goat-sdk/core';

declare class OpenseaPlugin extends PluginBase {
    constructor(apiKey: string);
    supportsChain: () => boolean;
}
declare const opensea: (apiKey: string) => OpenseaPlugin;

export { OpenseaPlugin, opensea };
